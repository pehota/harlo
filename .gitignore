# Throwaway trial fixture — its own nested git repo, disposable
harness-trial/

# Machine-local Claude Code settings
.claude/settings.local.json

# Private grading rubrics for harness testing — never commit
test-expectations/
